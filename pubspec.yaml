name: saveco_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  # sqflite: ^2.0.0+4
  path: ^1.8.3
  intl: ^0.20.1
  image_picker: ^0.8.7+4
  sqflite: any
  path_provider: any


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
